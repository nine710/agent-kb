# Cross-Source Review

Use one record per candidate-level relationship between claims from different
source packages. Keep local claims as `CLM-NNN`; use `src-NNN/CLM-NNN` for an
external claim.

## XSR-001

- candidate_id:
- status: no-overlap
- claim_refs:
- scope_conditions:
- resolution_basis:
- superseding_claim_ref:
